@model IEnumerable<ChildActionExample.Models.Product>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="container">
    <div class="row">
        <div class="col-sm-8 col-md-9">

            @foreach (var product in Model)
            {
                Html.RenderPartial("~/Views/Shared/_ProductDetails.cshtml", product);
            }

        </div>

        @* Will make a call back into the home controller *@

        @Html.Action("Categories")
        

        @* No real reason to use this version as it needs to be contained in a code block *@
        @*@{ Html.RenderAction("Categories"); }*@
        


    </div>
</div>